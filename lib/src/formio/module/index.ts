import iconClass, { ICONS } from './icons/iconClass';

const icons = {
    ICONS,
    iconClass
}

const material = {
    ICONS,
    iconClass
}

export default {
    framework: 'material',
    ICONS,
    iconClass
};
